{
  "name": "pppi-admin-panel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-5174}"
  },
  "dependencies": {
    "vite": "^8.1.1"
  }
}
